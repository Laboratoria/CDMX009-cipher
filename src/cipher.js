const cipher = {
  encode,
  decode
};

export default cipher;



function encode(string,offset){
  
  //crear la fórmula para codificar
}

function decode(string, offset){
  //crear formula para Decoficar
}