//Aquí va el código que tiene que ver con la interacción del DOM
//(seleccionar, actualizar y manipular elementos del DOM y eventos)
//Aquí debo invocar a cipher.encode(offset,string) y cipher.decode(offset,string)

import cipher from './cipher.js';

console.log(cipher);
